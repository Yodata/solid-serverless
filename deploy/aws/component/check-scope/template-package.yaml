AWSTemplateFormatVersion: '2010-09-09'
Description: 'check-scope validates event.object with event.scope, adding event.isValid
  {boolean}

  '
Globals:
  Function:
    Environment:
      Variables:
        CLIENT_ID:
          Ref: clientid
        DEBUG_LEVEL: debug
        NODE_ENV: production
    Runtime: nodejs8.10
    Timeout: 6
Parameters:
  clientid:
    Type: String
Resources:
  CheckScopeFunction:
    Properties:
      CodeUri: s3://yodata-solid-serverless-check-scope/55b18c9f1c84b3fd590d110eb8a66bf7
      Description: validates event.object with event.scope, adding event.isValid {boolean}
      FunctionName: check-scope
      Handler: index.handler
    Type: AWS::Serverless::Function
Transform: AWS::Serverless-2016-10-31
