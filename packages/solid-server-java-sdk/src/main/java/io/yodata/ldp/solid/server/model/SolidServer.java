package io.yodata.ldp.solid.server.model;

public class SolidServer {

    /*
    private ContainerHandler folder;
    private ResourceHandler file;

    public SolidServer(Store store) {
        folder = new ContainerHandler(store);
        file = new ResourceHandler(store);
    }

     */

}
