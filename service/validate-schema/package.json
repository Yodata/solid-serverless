{
  "name": "@yodata/validate-schema",
  "version": "0.0.1",
  "description": "what does it do?",
  "scripts": {
    "test": "npx jest --env node --bail",
    "deploy": "AWS_PROFILE=solid-dev arc deploy",
    "deploy:production": "AWS_PROFILE=production arc deploy --production"
  },
  "keywords": [],
  "author": "yodaveduran <dave@yodata.io> (https://yodata.io/)",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.1.1",
    "jest": "^29.1.2"
  }
}
